feat: AI Corrector — fix user message typos before agent processes

New layer between user message and agent:
  User msg → AI Corrector → Agent (detect_intent → collect → reply)

- corrector.py: fixes spelling, word completion, grammar
  "gdi bechna h" → "gaadi bechna hain"
  "jcB tipr 40 lac" → "JCB Tipper 40 lakh"
- Fast Python correction for known typos (no LLM needed)
- LLM correction for complex messages (scoped, no tools, no meaning change)
- Meaning change detection: rejects if intent/numbers flipped
- Skip short messages (Haan, Nahi, Ok, OTP, photo)
- Original text preserved in DB for audit (ai_correction event)
- Config flag: ai_corrector (default True)
- Wired into runner.py before _ai_respond
- 13 unit tests
